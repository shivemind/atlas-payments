name: Publish to Postman Cloud

on:
  push:
    branches: [main]
    paths:
      - 'postman/**'
      - 'openapi/**'
      - '.postman/**'
  workflow_dispatch:

jobs:
  publish:
    name: Sync to Postman Cloud
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Postman CLI
        run: curl -o- "https://dl-cli.pstmn.io/install/unix.sh" | sh

      - name: Login to Postman
        run: postman login --with-api-key ${{ secrets.POSTMAN_API_KEY }}

      - name: Push workspace to Postman Cloud
        run: postman workspace push -y

  test:
    name: Run API Tests
    needs: publish
    runs-on: ubuntu-latest
    steps:
      - name: Run Postman Collection
        uses: postmanlabs/postman-cli-action@v1
        with:
          command: 'collection run 52400725-ba9cb481-f991-49b3-b21b-9a879b8beeaa -e 78bf9ef1-d7c8-489e-9fab-d596668ccb62'
          api-key: ${{ secrets.POSTMAN_API_KEY }}
        continue-on-error: true

      - name: Summary
        if: always()
        run: |
          echo "### Postman Cloud Sync & Tests" >> $GITHUB_STEP_SUMMARY
          echo "- Workspace synced via Native Git" >> $GITHUB_STEP_SUMMARY
          echo "- Collection tests executed against 52400725-ba9cb481-f991-49b3-b21b-9a879b8beeaa" >> $GITHUB_STEP_SUMMARY