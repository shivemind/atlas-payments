apiVersion: v1
kind: Service
metadata:
  name: atlas-payments
  namespace: atlas-production
  labels:
    app: atlas-payments
spec:
  type: ClusterIP
  selector:
    app: atlas-payments
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
